import styled, { sizing } from "@xstyled/styled-components";

const Card = styled.div`
	padding: 16 8 8;
	margin: 60px auto 0 auto;
	box-shadow: default;
	background-color: gray-100;
	border: 1px solid;
	border-color: gray-200;
	${sizing}
`;

export default Card;
