import { Button } from "@components/Button";
import Providers from "../../containers/Providers";

const Login = () => {
	return (
		<Providers>
			<Button onClick={console.log}>Oieee</Button>
		</Providers>
	);
};

export default Login;
